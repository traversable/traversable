import type { symbol as __ } from "@traversable/registry"
import type { Symbol_optional as _ } from "./exports.js"

export * as fc from "./exports.js"
export { 
  /** 
   * ### {@link _ `Symbol_optional`}
   * 
   * {@link _ `Symbol_optional`} is a re-export from
   * {@link __.optional `registry.symbol.optional`}. 
   * 
   * It is re-exported only so that TypeScript can resolve the cross-package 
   * identifier. 
   * 
   * It is not meant to be used directly.
   * 
   * @deprecated use {@link __.optional `registry.symbol.optional`} instead.
   */
  Symbol_optional,
} from "./exports.js"
